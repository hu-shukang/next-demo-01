import Link from 'next/link';

const AddressViewerPart = () => {
  return (
    <div>
      <div>address viewer part</div>
      <Link href={'/user'}>to default</Link>
    </div>
  );
};

export default AddressViewerPart;
